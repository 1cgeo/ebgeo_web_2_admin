import React from 'react';
import { Typography } from '@mui/material';

const CatalogPage: React.FC = () => {
  return (
    <Typography variant="h4">Catálogo 3D</Typography>
  );
};

export default CatalogPage;